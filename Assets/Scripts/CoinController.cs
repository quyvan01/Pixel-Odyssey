using UnityEngine;

/////public class CoinController : MonoBehaviour
///{
    ///public int scoreValue = 1; // Value of the coin

  ///  private void OnTriggerEnter2D(Collider2D other)
    ///{
       // if (other.CompareTag("Player"))
       // {
       //     // Assuming the PlayerController script is attached to the player GameObject
        //    other.GetComponent<PlayerMovement>().AddScore(scoreValue);
//
         //   Destroy(gameObject); // Destroy the coin
      //  }/
  //  }
///}
